const recipes = require('./recipes');
const search = require('./search');
const user = require('./user');
const ownRecipes = require('./own-recipes');
module.exports = {
	recipes,
	search,
	user,
	ownRecipes,
};
